@using Microsoft.AspNetCore.Mvc.TagHelpers
@model PL.ViewModels.ChatViewModel

@{
    ViewData["Title"] = "Change Password";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@*<!-- Chat messages list -->
<div class="chat-messages">
    @foreach (var message in Model.MessageHistory)
    {
        <div class="chat-message">
            <div class="sender">@message.Sender.UserName</div>
            <div class="content">@message.Content</div>
            <div class="timestamp">@message.Timestamp.ToShortTimeString()</div>
        </div>
    }
</div>

<!-- Chat message input form -->
<form asp-controller="Chat" asp-action="SendMessage" method="post">
    <input type="hidden" asp-for="ChatMessage.ReceiverId" />
    <textarea asp-for="ChatMessage.Content"></textarea>
    <button type="submit">Send</button>
</form>*@

<div class="row">
    <div class="col-md-4">
        <h3>Contacts</h3>
        <ul id="contactsList" class="list-group">
            <!-- Populate this list dynamically with user contacts -->
        </ul>
    </div>
    <div class="col-md-8">
        <div id="chatArea" class="chat-area">
            <ul id="messagesList" class="list-unstyled">
                <!-- Messages will be appended here dynamically -->
            </ul>
        </div>
        <div class="input-group">
            <input type="text" id="messageInput" class="form-control" placeholder="Type your message here..." />
            <div class="input-group-append">
                <button id="sendButton" class="btn btn-primary">Send</button>
            </div>
        </div>
    </div>
</div>